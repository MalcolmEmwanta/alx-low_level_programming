#include <stdio.h>

/**
 * _strlen - to return the length of a string
 *@s: string to compare
 *
 *Return:return the length
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[1] != '\0'; i++)
	{}
	return (i);
}
